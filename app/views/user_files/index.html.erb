<p id="notice"><%= notice %></p>

<h1>User Files</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Desc</th>
      <th>File</th>
      <th>FileType</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_files.each do |user_file| %>
      <tr>
        <td><%= user_file.title %></td>
        <td><%= user_file.desc %></td>
        <td><%= user_file.file.filename %></td>
        <td><%= user_file.file.content_type %></td>
        <td><%= link_to 'Show', user_file %></td>
        <td><%= link_to 'Edit', edit_user_file_path(user_file) %></td>
        <td><%= link_to 'Destroy', user_file, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User File', new_user_file_path %>
